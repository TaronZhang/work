# 你的任务是：从招标文件（.docx）中提取指定章节内容，自动生成技术偏离表Excel。

## 输入
- 招标文件路径：${source_docx}
- 目标章节：${chapter}
- 输出Excel路径：${output_xlsx}

## 必须遵守的流程

### 第一步：读取与提取
1. 使用 python-docx 读取 ${source_docx} 的全部段落（含样式名）和全部表格
2. 定位 ${chapter} 的起止段落索引
3. 将章节内段落和表格的结构概览输出给用户

### 第二步：与用户确认边界
以下情况必须暂停，向用户提问，不得自行决定：
- 章节起止边界不明确
- "以下"合并块的具体范围界定不清
- 表格嵌入内容的字段呈现格式不确定
- 粒度划分不确定（标题级/段落级/子段落级）
- 哪些内容是"备注"需跳过
- 原文内容理解有歧义

### 第三步：逐条生成条目
条目结构如下：

```python
entry = {
    'bid': '招标文件技术条款（B列）',
    'resp': '投标文件技术条款（C列）',
    'deviation': '无偏离'
}
```

## 字段规则

### A列：序号
每填写一项数值加一，从1开始。

### B列：招标文件技术条款
- **完全复制原文，一字不改，不得截断或省略**
- 多层架构用换行符分隔
- ★星号必须保留
- 原文"投标人"保留不变
- **表格内容按行生成条目，格式：字段名：内容；字段名：内容**
- **长段落（如费用计算公式、示例等）必须完整摘抄，不得截断**

### C列：投标文件技术条款
格式：`应答：完全满足且无偏离。` + **完整原文内容**（仅做措辞清理）

**重要规则：C列应答必须完整包含原文内容，不得简化或截断**

**措辞清理（仅C列执行）：**
| 原文 | 替换为 |
|------|--------|
| ★ | （删除） |
| 投标人 | 我司 |
| 至少应对 | 应对 |
| 至少应包含 | 应包含 |
| 至少应包括 | 应包括 |
| 至少包括 | 包括 |
| 至少提供 | 提供 |
| 不少于N年 | N年 |
| 应有N年 | N年 |
| 需N年 | N年 |
| 包括但不限于 | 包括 |
| 包含且不限于 | 包含 |
| 要求具有 | 具有 |
| 要求具备 | 具备 |
| 至少N次 | N次 |
| 至少N个 | N个 |
| （剩余孤立"至少"） | （删除） |

**服务人员要求特殊格式：**
删除序号(1)(2)，格式改为：`[原文内容]我司服务人员满足[原文内容]`

**表格行应答格式：**
格式：`应答：完全满足且无偏离。字段名：我司执行内容；字段名：我司提供内容。`

### D列：偏离说明
全部写"无偏离"

## 条目划分规则

1. **表格**：每行一个条目，格式为"字段名：内容；字段名：内容"，表头行跳过
2. **标题下多段落**：一标题一段落为一条
3. **"以下"合并**：文本出现"以下"→ 后续连续子内容合并为一条，子项保留 a) b) c) 标签
4. **"内容包括但不限于以下范围"** → 合并为一条
5. **"涵盖以下功能"** → 合并为一条
6. **备注**：跳过
7. **纯标题行**：跳过
8. **得分算法表格**：每行一个条目，包含得分类型、算法、证明材料三个字段
9. **扣分类型表格**：每行一个条目，包含扣分类型、算法、证明材料字段

## 子项序号规则
- 原文无编号 → 分配 a) b) c)
- 原文有编号 → 保留

## Excel输出规范

```python
import openpyxl
from openpyxl.styles import Alignment, Font, Border, Side, PatternFill

thin = Border(left=Side('thin'), right=Side('thin'), top=Side('thin'), bottom=Side('thin'))
hfont = Font(name='宋体', size=11, bold=True)
cfont = Font(name='宋体', size=10)
hfill = PatternFill(start_color='D9E1F2', end_color='D9E1F2', fill_type='solid')

ws.column_dimensions['A'].width = 8     # 序号
ws.column_dimensions['B'].width = 70    # 招标文件技术条款
ws.column_dimensions['C'].width = 70    # 投标文件技术条款
ws.column_dimensions['D'].width = 15    # 偏离说明

# 表头: 序号 | 招标文件技术条款 | 投标文件技术条款 | 偏离说明
# 加粗、浅蓝底色、居中、冻结首行
ws.freeze_panes = 'A2'
```

## 生成后验证

自动检查并报告：
- C列0残留 ★
- C列0残留"至少/不少于/包括但不限于/要求具有/要求具备"
- B列★仅保留原文自带项
- 序号连续无跳号
- 子项标签完整
- C列投标人→我司全部替换，B列原样保留
- **B列无截断省略（长段落必须完整）**
